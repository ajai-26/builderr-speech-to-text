# Local ASR engine for the baseline / your submission (all local, no cloud at scoring).
faster-whisper>=1.0        # or whisper.cpp bindings, parakeet (nemo/onnx), qwen3-asr via transformers
# Corpus audio fetch (used by scripts/fetch_audio.py on the Action, not at scoring time):
datasets>=2.18
soundfile>=0.12
